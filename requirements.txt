anthropic>=0.50
gtts>=2.5
pillow>=10
fastapi>=0.115
uvicorn[standard]>=0.30
gunicorn>=22.0
python-multipart>=0.0.9
python-dotenv>=1.0
piper-tts>=1.4
# Production-tier (1M-user scale) — only loaded when DATABASE_URL /
# S3_BUCKET are configured; the dev path keeps working without them.
psycopg[binary,pool]>=3.2
boto3>=1.35
bcrypt>=4.2
pyjwt>=2.9
redis>=5.0
# `rq` lives in requirements-optional.txt — the v1.2 smoke test asserts
# the not-installed fallback path, and prod deploys install it
# explicitly via `pip install -r requirements-optional.txt` only when
# REDIS_URL is provisioned. Keeping it here would break smoke CI.
PyMuPDF>=1.24    # PDF ingest (renders pages to PNG without poppler)
